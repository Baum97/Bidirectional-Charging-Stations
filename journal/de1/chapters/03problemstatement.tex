\subsection{Problem Statement}
\label{subsec:problem}
Given a set of candidate charging locations, we aim to determine a deployment configuration that minimizes travel and waiting times while respecting grid capacity constraints. The challenge is multi-objective and dynamic, since mobility demand and feeder loading change throughout the day. We formulate this as a constrained optimization problem informed by simulation outputs from both traffic and grid models.
